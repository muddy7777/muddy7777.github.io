---
title: Prompts
description: 常用 prompt 模板索引。
---

Prompt 的目标不是写得长，而是让 agent 明确：任务是什么、上下文在哪里、约束是什么、交付物如何验收。

## Prompt 模板的基本结构

```text
目标：
上下文：
输入：
约束：
工作方式：
输出格式：
验收标准：
```

## 维护原则

- 每个 prompt 只解决一类任务。
- 模板中保留变量位，不把一次性信息写死。
- 输出格式越具体，后续检查越容易。
- 对复杂任务，prompt 应该引用上下文页面，而不是复制整站内容。

